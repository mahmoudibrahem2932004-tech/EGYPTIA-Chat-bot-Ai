name: Sync to Hugging Face Hub
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to HF
        run: |
          git config --global user.email 'mahmoudibrahem2932004@gmail.com'
          git config --global user.name 'MahmoudEgEG'
          # التوكن محطوط هنا مباشرة عشان يرفع عافية
          git push --force https://MahmoudEgEG:Hf_HNbTSsBuAnUhCSfeMQWupYQKFZsLmSnfoh@huggingface.co/spaces/MahmoudEgEG/EGYPTIA-Chat-bot-Ai main